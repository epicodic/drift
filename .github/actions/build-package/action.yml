name: Build package
description: Install Qt6/KWin build tooling and run `make package`.
runs:
  using: composite
  steps:
    - uses: ./.github/actions/setup-toolchain

    - name: make package
      shell: bash
      run: make package
